# 貢獻指南

感謝你有興趣為 crypto-quant-model 做出貢獻！

## 開發流程

1. Fork 本專案
2. 建立功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交變更 (`git commit -m 'Add some amazing feature'`)
4. 推送到遠端 (`git push origin feature/amazing-feature`)
5. 開啟 Pull Request

## 程式碼規範

- 使用 `black` 格式化
- 使用 `flake8` 進行 lint
- 撰寫單元測試並確保通過

```bash
black .
flake8 .
pytest
```

## Issue 與 PR

請使用 `.github/ISSUE_TEMPLATE` 與 `PULL_REQUEST_TEMPLATE.md` 提供的範本。
